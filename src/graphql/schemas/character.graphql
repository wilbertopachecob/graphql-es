type Query {
  getCharacters: [Character]
}

type Game {
  _id: ID
  name: String
}

type Character {
  _id: ID
  name: String
  race: Race
  genre: Genre
  games: [Game]
}

enum Race {
  HYLIAN
  GERUDO
  FAIRY
}

enum Genre {
  MALE
  FEMALE
}
