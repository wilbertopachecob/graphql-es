type Query {
  getGames: [Game]
}

type Game {
  _id: ID
  title: String
  developers: [Developer]
}

type Mutation {
  addGame(game: NewGameInput!): String!
}

input NewGameInput {
  title: String!
  developers: [String!]!
}
